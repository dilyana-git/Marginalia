APP_NAME=Marginalia
APP_ENV=local                          # local | prod
APP_API_KEY=                           # if set, requires X-Api-Key header on /api/*
DATABASE_URL=sqlite:///./marginalia.db
LOG_LEVEL=INFO

# Newsletter — Claude summarisation
ANTHROPIC_API_KEY=

# Newsletter — SMTP
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=
MAIL_TO=
